# Client-side "environment" config for this static site.
# Loaded by assets/js/env.js and exposed as window.ENV.
# Toggle TEST_MODE to switch the application/checkout flow between
# demo behaviour (accepts test card numbers, dumps the submission to
# show.html as JSON) and normal production behaviour.
#
# NOTE: this is named "env.txt" rather than ".env" because many dev
# servers and static hosts refuse to serve dot-prefixed files over
# HTTP by default, which would silently break this toggle.

TEST_MODE=true

# Base URL of the deployed "idta-upload" Cloudflare Worker (see /worker).
# Uploaded documents/signature go to the "idta-html" R2 bucket through this
# Worker, and show.html fetches them back through it too. Leave blank to
# fall back to local (non-R2) preview behaviour.
R2_WORKER_URL=

